# Exact versions of every package (direct and transitive) that the tests and
# the real collection run were verified against. Used as a pip *constraints*
# file (-c): it caps what pip may install but never adds packages by itself.
#
#   pip install -c constraints.txt -e ".[dev]"
#
# Why: CI and Docker used to install the newest release of everything, so a
# new upstream version could break a run with no change on our side (this
# happened: SQLAlchemy 2.1 changed the default PostgreSQL driver).
#
# Updating is a deliberate step: see README > "Dependency versions".
# Verified on Python 3.13 (Windows) and resolvable on Python 3.12 / Linux,
# which is what CI and Docker use.
aiohappyeyeballs==2.7.1
aiohttp==3.14.3
aiosignal==1.4.0
annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.15.1
attrs==26.1.0
certifi==2026.7.22
charset-normalizer==3.5.1
click==8.5.0
colorama==0.4.6
fastapi==0.141.1
firecrawl-py==4.43.0
frozenlist==1.8.0
greenlet==3.5.6
h11==0.16.0
httpcore2==2.12.0
httpcore==1.0.9
httpx2==2.12.0
httpx==0.28.1
idna==3.19
iniconfig==2.3.0
jiter==0.16.0
multidict==6.8.0
nest-asyncio==1.6.0
openai==3.13.0
packaging==26.3
pluggy==1.6.0
propcache==0.5.4
psycopg2-binary==2.9.13
pydantic==2.13.5
pydantic_core==2.46.5
Pygments==2.21.0
pytest==9.1.1
python-dotenv==1.2.3
requests==2.34.2
sniffio==1.3.1
SQLAlchemy==2.0.54
starlette==1.6.0
truststore==0.10.4
typing-inspection==0.4.4
typing_extensions==4.16.0
urllib3==2.8.0
uvicorn==0.53.0
websockets==17.1
yarl==1.25.1
